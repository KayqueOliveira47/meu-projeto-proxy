[project]
name = "meu-projeto-proxy"
version = "0.1.0"
description = "Proxy de Open Finance para Pluggy"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
    "requests>=2.31.0",
    "httpx>=0.27.0",
]

[tool.vercel]
entrypoint = "main.py:app"
